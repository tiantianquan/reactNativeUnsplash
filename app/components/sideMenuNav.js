'use strict'
import React from 'react'

import {
  ScrollView,
  View,
  Text
} from 'react-native'

const SideMenuNav = React.createClass({
  render () {
    return (
      <ScrollView>
        <Text>asdfsda</Text>
      </ScrollView>
    )
  }
})

export default SideMenuNav
