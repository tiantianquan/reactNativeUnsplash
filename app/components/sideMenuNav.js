'use strict'
import React from 'react-native'

const {
  ScrollView,
  View,
  Text
} = React

const SideMenuNav = React.createClass({
  render () {
    return (
      <ScrollView>
        <Text>asdfsda</Text>
      </ScrollView>
    )
  }
})

export default SideMenuNav
