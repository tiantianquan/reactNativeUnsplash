'use strict'
/**
 * source 前缀URL
 * @type {String}
 */
const SOURCE_PREFIX = 'https://source.unsplash.com'

/**
 * api 前缀URL
 * @type {String}
 */
const API_PREFIX = 'https://api.unsplash.com'


export default {

  /**
   * 随机图片URL
   * @type {RegExp}
   */
  RANDOM_URL: `${SOURCE_PREFIX}/random`,



}
